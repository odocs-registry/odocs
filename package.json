{
  "name": "odocs",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "eslint": "^8.40.0",
    "prettier": "^2.8.0",
    "typescript": "^5.0.0"
  }
}
